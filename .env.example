# BoardPractice Environment Configuration
# Copy this file to .env and update with your values
# IMPORTANT: Never commit .env file to version control!

# ======================
# Database Configuration
# ======================
# Database host (default: localhost)
DB_HOST=localhost

# Database port (default: 3306)
DB_PORT=3306

# Database name (default: board)
DB_NAME=board

# Database username
# IMPORTANT: In production, create a dedicated user with minimal privileges
# DO NOT use root user in production!
DB_USERNAME=root

# Database password
# IMPORTANT: Use a strong password in production
DB_PASSWORD=zen911!@

# Enable SSL for database connection (recommended for production)
# Set to 'true' in production, 'false' for local development
DB_USE_SSL=false

# ======================
# Redis Configuration
# ======================
# Redis host (default: localhost)
REDIS_HOST=localhost

# Redis port (default: 6379)
REDIS_PORT=6379

# Redis password (leave empty if no password is set)
# IMPORTANT: In production, always set a strong Redis password
# Configure in redis.conf: requirepass your-strong-password
REDIS_PASSWORD=

# ======================
# Application Configuration
# ======================
# Server port (default: 8080)
SERVER_PORT=8080

# ======================
# Production Deployment Checklist
# ======================
# Before deploying to production, ensure:
# 1. ✓ Set DB_USERNAME to a dedicated application user (not root)
# 2. ✓ Set strong DB_PASSWORD
# 3. ✓ Enable DB_USE_SSL=true and configure SSL certificates
# 4. ✓ Set strong REDIS_PASSWORD and configure Redis requirepass
# 5. ✓ Use HTTPS for the application (configure reverse proxy/load balancer)
# 6. ✓ Review and restrict DB user permissions (GRANT SELECT, INSERT, UPDATE, DELETE only)
# 7. ✓ Enable Redis persistence (RDB/AOF) for session durability
# 8. ✓ Configure firewall rules to restrict database and Redis access
# 9. ✓ Set up monitoring and logging for security events
# 10. ✓ Regular security updates and dependency scanning
